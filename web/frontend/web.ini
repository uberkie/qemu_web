[uwsgi]
module = wsgi:app # Python module name
master = true
processes = 5  # Number of uWSGI worker processes
socket = web.sock  # Path to the socket file uWSGI will create
chmod-socket = 664  # Permissions for the socket file
vacuum = true
die-on-term = true
